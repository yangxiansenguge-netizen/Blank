<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blank.app.mapper.FriendMapper">
    <select id="selectFriends" resultType="com.blank.app.entity.Friend">
        SELECT * FROM friends WHERE (user_id = #{userId} OR friend_id = #{userId}) AND status = 'accepted'
    </select>
    <select id="selectPendingRequests" resultType="com.blank.app.entity.Friend">
        SELECT * FROM friends WHERE friend_id = #{userId} AND status = 'pending'
    </select>
    <select id="selectById" resultType="com.blank.app.entity.Friend">
        SELECT * FROM friends WHERE id = #{id}
    </select>
    <select id="countExisting" resultType="long">
        SELECT COUNT(*) FROM friends
        WHERE (user_id = #{userId1} AND friend_id = #{userId2}) OR (user_id = #{userId2} AND friend_id = #{userId1})
    </select>
    <select id="checkIsFriend" resultType="long">
        SELECT COUNT(*) FROM friends WHERE status = 'accepted' AND
        ((user_id = #{userId1} AND friend_id = #{userId2}) OR (user_id = #{userId2} AND friend_id = #{userId1}))
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO friends (user_id, friend_id, status) VALUES (#{userId},#{friendId},#{status})
    </insert>
    <update id="updateStatus">
        UPDATE friends SET status = #{status} WHERE id = #{id}
    </update>
    <delete id="deleteById">
        DELETE FROM friends WHERE id = #{id}
    </delete>
    <delete id="deleteByUserId">
        DELETE FROM friends WHERE user_id = #{userId} OR friend_id = #{userId}
    </delete>
</mapper>
