<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blank.app.mapper.NotificationMapper">
    <select id="selectById" resultType="com.blank.app.entity.Notification">
        SELECT * FROM notifications WHERE id = #{id}
    </select>
    <select id="selectByUserId" resultType="com.blank.app.entity.Notification">
        SELECT * FROM notifications WHERE user_id = #{userId} ORDER BY created_at DESC
    </select>
    <select id="countUnread" resultType="long">
        SELECT COUNT(*) FROM notifications WHERE user_id = #{userId} AND is_read = 0
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notifications (user_id, type, title, content, postcard_id, is_read)
        VALUES (#{userId},#{type},#{title},#{content},#{postcardId},#{isRead})
    </insert>
    <update id="updateById">
        UPDATE notifications
        <set>
            <if test="isRead != null">is_read = #{isRead},</if>
        </set>
        WHERE id = #{id}
    </update>
    <update id="markAllRead">
        UPDATE notifications SET is_read = 1 WHERE user_id = #{userId} AND is_read = 0
    </update>
</mapper>
